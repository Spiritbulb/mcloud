{
  "name": "mcloud",
  "version": "1.1.0",
  "private": true,
  "packageManager": "npm@11.7.0",
  "workspaces": [
    "apps/web",
    "apps/storefront",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "obsidian": "node scripts/obsidian-graph.js",
    "update-nuru": "cd apps/nuru && npx eas-cli update --branch production --environment production --message \"new stuff\""
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
